export default function TermsPage() {
  return (
    <main style={{ padding: "32px", color: "#e6edf3", fontFamily: '"SF Pro Display", "Segoe UI", sans-serif' }}>
      <h1>Terms of Use</h1>
      <p>
        TON AI is an assistant interface. Any wallet action, swap or transfer must be explicitly confirmed by the user.
      </p>
    </main>
  );
}
